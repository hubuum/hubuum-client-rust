# hubuum_client_derive

Procedural macros used by `hubuum_client` to define Hubuum API resources.

This crate is published so `hubuum_client` can be released to crates.io, but it is primarily an implementation detail of the main client crate.
