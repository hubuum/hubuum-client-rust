[package]
name = "e2e_client"
version = "0.0.0"
edition = "2024"
rust-version = "1.88"
publish = false

[features]
integration-tests = []

[dependencies]
hubuum_client = { path = "..", features = ["async", "blocking"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
